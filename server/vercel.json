{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/node" }
  ],
 "routes": [
    { "src": "/quiz/:quizId/:quizType", "dest": "/index.html" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
