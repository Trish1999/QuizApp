{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/v1/user/(.*)", "dest": "/" },
    { "src": "/api/v1/quiz/(.*)", "dest": "/" },
    { "src": "/(.*)", "dest": "/                    " }
  ]
}
